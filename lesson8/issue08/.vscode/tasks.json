{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "g++ ${file} -std=c++11 `pkg-config --cflags --libs opencv4` && ./a.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}